# BoundChrome Role Matrix — Tasks

1. Spec + Delta (this folder + `docs/delta/20_micro/boundchrome-role-matrix.md`).
2. Core API: `SpzUiThemeRole`, `SpzUiThemeRoleTag`, `ApplyBoundChromeRolesUnder`, optional root registry.
3. Pilot: `SD_InputPanel_UI`, `ControlNetUnit_UI` (+ download), `SD_WorkflowOptionsRibbon_UI`.
4. EditMode contracts + wiring audit.
