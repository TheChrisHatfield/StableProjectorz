StableProjectorz — Change log
==============================

Project version (Unity Player Settings): 2.4.5
This file tracks changes on top of that baseline until the next release bump.

-------------------------------------------------------------------------------

[Unreleased]  |  Date: 2026-04-25  |  Topic: SPZ GO bridge hardening, startup stability, WebUI GPU selection
-------------------------------------------------------------------------------

Summary
  Stabilized Blender/SPZ GO exchange and in-app add-on behavior, reduced startup
  flicker from add-on Python server launch/restart paths, and improved SD GPU
  switching so Restart WebUI applies selected CUDA device more reliably.

SPZ GO / Blender bridge
  * External/Blender_SpzBridge/__init__.py
    - Export behavior:
      • selection present => export active object (fallback first selected)
      • no selection => default full-scene FBX (use_selection=False)
    - Added texture auto-assign after SPZ import:
      • find best exchange texture beside FBX
      • ensure UV Map -> Image Texture -> Principled Base Color node path
      • apply to selected/imported mesh objects
    - Added maps-only operator/button:
      • spz.go_apply_maps_only ("Apply SPZ maps only")
      • refreshes maps from SPZ and applies to selected meshes without FBX reimport

SPZ GO in-app add-on / RPC reliability
  * Assets/StreamingAssets/Addons/StableProjectorzGO/__init__.py
    - Added detailed import/export diagnostics:
      raw/normalized path, exists/size checks, RPC payload/response, traceback
  * Assets/_gm/Features/AddonSystem/AddonUI_MGR.cs
    - SPZ GO native in-process import/export fallback to Python callback on failure
  * Assets/_gm/Features/AddonSystem/Addon_SocketServer.cs
    - Method-aware command timeout:
      • default: 10s
      • long ops (import/export/save/load/texture export): 120s
  * Assets/StreamingAssets/AddonSystem/spz.py
    - Method-aware long-op timeout aligned to 120s
  * Assets/_gm/Features/3D Models/ModelsHandler3D_ImportHelper.cs
    - .fbx export-to-path: prefer binary-capable scene re-export when live model exists
    - clearer fallback/logging when only cached bytes exist

Startup / launch behavior
  * Assets/_gm/Features/AddonSystem/Addon_MGR.cs
    - Add-on Python server launch hidden in player builds (less console flicker)
    - Added _autoRestartWithAddonsOnServerFail (default false) so Run_with_Addons
      relaunch is opt-in (avoids surprise restart/flicker)

Stable Diffusion GPU selection
  * Assets/_gm/Features/StableDiffusion/Webui/Launch_WebUI_bat_File.cs
    - Hardened GetLaunchPathWithGpuSetting:
      • robust path + appended-arg parsing
      • prefer direct launch.py execution when available
      • use venv python if present, else PATH python fallback
      • preserve extra args in COMMANDLINE_ARGS
      • clearer logs for direct/fallback launch path and CUDA selection

Notes for testers
  * In-app SPZ GO Import/Export: use new console diagnostics to identify failures.
  * Blender SPZ GO: validate both "Import from StableProjectorz" and
    "Apply SPZ maps only" on selected mesh.
  * GPU switching: set SD GPU in Settings, click Restart WebUI (apply GPU),
    confirm [LaunchWebUI] logs show selected CUDA device and direct launch path.

-------------------------------------------------------------------------------

[Unreleased]  |  Date: 2026-04-07  |  Topic: Add-on API — editor layout, display, UI chrome (refactor notes)
-------------------------------------------------------------------------------

Summary
  Expanded spz.cmd.* JSON-RPC (TCP 5555) + mirrored FastAPI + shipped spz.py for
  add-on control of: skeleton column widths (Global_Skeleton_UI), window/fullscreen
  mode, ribbon tabs, cursor lock, global UI scale (skeleton CanvasScaler), named
  GameObject visibility, viewport status line, EventSystem. get_api_capabilities
  reports addon_rpc_version 1.6 — sync method list with handlers when refactoring.

Refactor / maintenance
  * Addon_SocketServer: BuildAddonApiCapabilities must match TryExecute* early paths
    (TryExecuteEditorLayoutCommand, TryExecutePlayerDisplayCommand,
    TryExecuteUiChromeCommand) + FastPath switch for other spz.cmd.
  * ExecuteCommand uses @params ??= new JObject() — omitting params is safe.

Key new C# (under Assets/_gm unless noted)
  * Global_Skeleton_UI.cs — side panel LayoutElement snapshot + visibility.
  * Addon_SocketServer.cs — RPC dispatch + validation.
  * SpzPlayerDisplay_API.cs, SpzCursor_API.cs, SpzUiChromeOps.cs, SpzUiChromeRegistry.cs
  * TabsGroup_UI.cs, CommandRibbon_UI.cs — ribbon tab discovery / programmatic switch.

Scene
  * UI_Global_Skeleton.unity — SpzUiChromeRegistry on skeleton canvas root (_extraTargets).

StreamingAssets/AddonSystem
  * spz.py — api.editor, api.display, api.ui_chrome
  * http_server.py — /api/v1/editor/layout, display/mode, chrome/*

Risks
  * UI scale: session baseline _referenceBaseline in SpzUiChromeOps is one-shot;
    scaler must be Scale With Screen Size on skeleton.
  * set_ui_target_active / set_event_system can disable core UI input if misused.

-------------------------------------------------------------------------------

[Unreleased]  |  Date: 2026-04-05  |  Topic: Command ribbon tab strip (many add-ons, resize)
-------------------------------------------------------------------------------

Summary
  The right-panel command ribbon tab row adapts when many add-on tabs load or when
  the panel width changes, instead of breaking layout by forcing equal flex widths
  against per-tab LayoutElement.minWidth.

CommandRibbon_UI.cs
  * PatchTabStripResponsiveLayout — childForceExpandWidth false (childControlWidth
    true) so label-driven mins work until rebalanced.
  * RebalanceStripTabMinWidthsIfOverflowing — proportional scale when sum of tab
    min widths exceeds strip budget; floors 36px then 28px; iterative pass down to
    1px when still over budget (guarded, stops on no progress).
  * HarmonizeStripTabTypography — calls rebalance after per-tab min width from labels.
  * Update — polls strip RectTransform width; >1px change triggers RefreshTabStripLayout;
    missing strip resets last width tracking.
  * FindFirstOtherStripTabForStyleReference — only direct children of the strip.
  * QueueTabStripRebuildNextFrame — coalesces next-frame rebuild coroutines; sequence
    id in finally avoids clearing the active coroutine handle when a superseded run
    finishes after a newer one was started.
  * EnsurePaintTabExists — uses QueueTabStripRebuildNextFrame like add-on refresh.

Notes for testers
  * Many add-ons + long titles: narrow/widen right panel; expect single row + ellipsis.
  * Horizontal ScrollRect on tab row: next-frame pass still scrolls right for new tabs.

-------------------------------------------------------------------------------

[Unreleased]  |  Date: 2026-03-25  |  Topic: Smudge brush tool
-------------------------------------------------------------------------------

Summary
  Adds a smudge brush as a third tool mode alongside paint and erase in the
  inpaint brush direction strip. Smudging blends neighboring pixel colors under
  the brush in real time during the stroke.

New files
  * BrushToolMode.cs — Paint, Smudge, Erase enum used by the direction toggle
    strip and paint pipeline.

Brush direction UI (SD_BrushRibbon_UI_Direction.cs, BrushRibbon_UI_Direction.cs)
  * BrushRibbon_UI_Direction gains isSmudge, toolMode, SetToolMode(BrushToolMode).
  * SD_BrushRibbon_UI_Direction.CreateSmudgeToggle_IfNeeded() programmatically
    clones the brush toggle, inserts it between paint and erase, re-anchors all
    three to vertical thirds, loads icon_smudge sprite.
  * Cursor color set to gray when smudge is active.

Smudge compute shader (ApplyBrushStroke_IntoMask.compute)
  * New CSSmudge kernel: 5x5 weighted-average blur with spacing proportional to
    brush size, applied only where the R8 brush stroke has coverage.
  * Reads from _SmudgeSourceCopy (snapshot), writes to _PaintedMask.
  * Reuses apply_brush_stroke_smudge() from Brush_ApplyFinalStroke_ToMask.cginc.

C# wiring
  * ApplyBrushStroke_ToUvMask.Apply_smudge_to_ColorBrushTex() — copies paint
    target to temp RT, dispatches CSSmudge with BLEND_RGBA_ONCE + SMUDGE_MODE.
  * Inpaint_MaskPainter.OnRenderIntoCurrTex_please() — when smudge active,
    dispatches smudge compute each frame during drag (undo on first frame).
  * Inpaint_MaskPainter.OnFinal_ApplyIncomingVals_intoMask() — skips normal
    color-apply when smudge; fires stroke-end event and re-render.
  * Inpaint_MaskPainter.getBrushStrength() — returns positive opacity for smudge.
  * SD_WorkflowOptionsRibbon_UI — exposes isSmudge and brushToolMode.

Notes for testers
  * Smudge only affects inpaint color painting (not projection masks or backgrounds).
  * If the smudge icon does not appear, assign icon_smudge.png as a sprite in a
    Resources folder or via the serialized field on the prefab.
  * Undo captures the paint target on the first frame of the smudge stroke.

-------------------------------------------------------------------------------

[Unreleased]  |  Date: 2026-03-11  |  Topic: Paint layer stack / inpaint → mesh / SD bridge
-------------------------------------------------------------------------------

Summary
  Multi-layer inpaint display and Stable Diffusion capture now follow the same
  final blit path as the proven single-layer case, with clearer fallbacks and
  resolution alignment to the mesh accumulation texture.

Inpaint / layer display (Inpaint_MaskPainter.cs)
  • ApplyColorLayer_To_UV_Textures: unified flow — resolve a single `source`
    RenderUdims, then one EntireColorLayer blit to accumulation (same pattern
    as Count == 1 using ActiveLayer.Content).
  • CompositeVisibleLayersIntoTemp: builds the multi-layer `source` by blitting
    each visible layer onto a cleared temp with EntireColorLayer_BlitApply (same
    material as single-layer), bottom→top, per-layer opacity; active layer can
    use APPLY_LATEST_BRUSH_TOO while painting.
  • Removed dependency on PaintLayerStack_MGR.CompositeTo for the mesh display
    path (avoids runtime-created stack manager often having no compositor shader
    assigned; CompositeTo / PaintLayer_CompositeBlend remains for other uses).
  • GetLayerCompositeOrFallback: multi-layer path uses CompositeVisibleLayersIntoTemp
    for consistency with viewport/SD mask source; fallbacks to active then layer 0.
  • maskResolution(): prefers accumulation texture width/height when UDIM slice
    count matches; if accumulation not ready, uses SceneResolution_MGR.resultTexQuality
    so layer RTs align with scene UV quality (reduces multi-blit / scale issues).
  • Earlier: removed Inpaint_NoColor + SD-prep early return that skipped layer
    blit during capture; EnsureBottomLayerHasSceneForComposite + scene buffer
    setup for multi-layer; warnings when composite temp or visible Content missing.

Layer stack manager (PaintLayerStack_MGR.cs)
  • CanCompositeLayers public property (_compositeBlendMat != null).
  • CompositeTo: if stack _resolution unset, adopt from first visible layer
    before allocating ping-pong temps; if temps still null, clear dest and log
    instead of leaving stale mask data.

Renderer (Objects_Renderer_MGR.cs)
  • Apply_InpaintSketch_ColorLayer: when layer count > 1, always run
    ApplyColorLayer_To_UV_Textures (UsualView still required); single-layer
    still gated on allowed_to_showBrushMask() so inpaint ribbon modes behave
    as before.

Notes for testers
  • After resolution changes, click in viewport or reload model once so brush
    InitTextures / EnsureResolution refresh layer buffers if sizes changed.
  • If PaintLayer_CompositeBlend fails to load, mesh display no longer depends
    on it; Collapse / mask helpers that still call CompositeTo may log warnings.

Documentation / continual learning
  • Context_Ref/REVIEW_Codebase_Summary.md (sibling folder) — sections 4–5, 9–10
    updated for this paint/layer pipeline.
  • Context_Ref/CONTINUAL_LEARNING.md — pointer to StableProjectorz/continual-learning/.
  • continual-learning/paint-layers-sd-bridge.md — Pattern lines for layer/display/SD
    (folder gitignored).
  • continual-learning/build-stability.md — stub for batch/build patterns (.cursor rule).

-------------------------------------------------------------------------------

Multi-layer paint → SD capture (2026-03-21)
-------------------------------------------------------------------------------

  • EntireColorLayer_BlitApply.shader: Blend One OneMinusSrcAlpha, One One —
    RGB premultiplied-over; alpha additive so CompositeVisibleLayersIntoTemp
    matches PaintLayer_CompositeBlend (fixes weak composite vs accumulation for SD).
  • Inpaint_MaskPainter: forStableDiffusionCapture skips save guard during capture.
  • Objects_Renderer_MGR.EnsureInpaintColorLayerAppliedForCapture: capture flag +
    GL.Flush after ShowFinalMat.
  • SD_GenRequests_Helper: EnsureInpaint + WaitForEndOfFrame before img2img payload
    and before view upscale when genData is null.
  • SD_Generate_PayloadMaker: EnsureInpaint before texture read where applicable.

  Unity packages: Packages/manifest.json and packages-lock.json had no local
  changes at this update (already in sync on main).

-------------------------------------------------------------------------------

Layer rename UI & enumerated collapse names (2026-03-22)
-------------------------------------------------------------------------------

Paint tab — layer list (PaintTab_LayersPanel_UI.cs)
  • Stack still supplies default Layer 1, Layer 2, …; DisplayBlock = read-only label.
  • Click name → EditBlock (TMP_InputField); Enter (onSubmit) commits SetLayerName;
    Escape or focus loss cancels without save.
  • Visibility button also SetActiveLayer so eye can pick active layer.
  • OnActiveLayerChanged → RefreshActiveHighlight only (not full rebuild) so rename
    input is not destroyed on active change.
  • Unity 6 TMP: SelectAll() protected — use onFocusSelectAll on the field.

Layer stack (PaintLayerStack_MGR.cs)
  • SetLayerName, DefaultLayerDisplayName (trim, max length, empty → default).
  • ConsumeNextDefaultCollapseLayerName → Collapse 1, Collapse 2, …
  • CollapseVisibleLayersIntoOne uses enumerated names, not fixed "Collapsed".

Save / load (SerializationObjects.cs + stack Save/Load)
  • PaintLayerStack_SL.nextCollapseNumber; 0 in old saves → InferNextCollapseNumber
    (Collapse N + legacy "Collapsed").

Inpaint (Inpaint_MaskPainter.cs)
  • CollapseLayersIntoScene uses ConsumeNextDefaultCollapseLayerName for the
    single merged layer name (same counter as paint-tab collapse).

-------------------------------------------------------------------------------

Paint undo / redo (session) + Settings UI (2026-03-22)
-------------------------------------------------------------------------------

New module Assets/_gm/Features/Paint/Undo/
  • PaintUndo_MGR: pre-stroke snapshot (GPU copy → async readback → pack → Deflate);
    undo/redo = inflate then amortized per-UDIM GPU upload in LateUpdate;
    PaintUndo_Scheduler: EWMA, LAVD-style aging, optional UCB1 budget.
  • PaintUndo_Input: Ctrl+Z / Ctrl+Y (Cmd+Z on Mac), input-field guard.
  • PaintUndo_Storage: ring buffer; max depth from settings (cap 16).
  • SnapshotRecord + PaintUndo_Compress: lossless format + Deflate;
    TryBuildUncompressedBlob + off-thread Deflate on capture / redo snapshot.
  • LinearSliceUploadOrder for restore upload order (was VisibleFirstOrder).

Hooks / safety
  • Inpaint_MaskPainter: SchedulePreStrokeCapture before brush apply; EnsureExists;
    GetPaintTarget_Undo for restore.
  • MaskPainter: block inpaint while PaintUndo_MGR.BlocksNewStroke.

TextureTools_SPZ.RenderTexture_to_Texture2DList_Async
  • Callback after all slice readbacks finish (incl. errors) via response counter.

Settings (Settings_MGR.cs / Settings_UI.cs)
  • PlayerPrefs: paintUndo_enabled, paintUndo_maxDepth (1–16); restore defaults;
    SyncPaintUndoOnOffLabel for ON/OFF text when toggle set from code.
  • Runtime paint-undo row: Toggle + Image same GameObject, green ON state,
    checkmark, ON/OFF label; max-depth field row.
  • _paintUndoSettingsRowsCreated only after successful row build.
  • VSync runtime row: 112×28 hit area, checkmark, navigation None, label no raycast.
  • SD GPU + paint-undo depth labels: raycastTarget false.
  • AddToggleCheckmarkGraphic helper for runtime toggles.

Documentation
  • docs/PAINT_UNDO_SPEC.md, docs/UNDO_INTEGRATION.md, docs/PAINT_UNDO_PROFILING.md

-------------------------------------------------------------------------------

Paint undo — Thompson / contextual / capture bandit + scratch split (2026-03-24)
-------------------------------------------------------------------------------

PaintUndo_Scheduler.cs
  • RestoreBudgetPolicy: FixedMiddle, Ucb1, Thompson; Thompson per bucket + decay
    on BeginRestoreSession; RegisterRestoreBanditObservation (UCB continuous /
    Thompson binarized); RegisterUcbReward obsolete.
  • Capture bandit: discrete arms for readback inflight + yield frames; cold-start
    per bucket then Thompson; hitch window after capture updates posteriors.

PaintUndo_MGR.cs
  • _scratchRestore vs _scratchCapture[0/1] ping-pong; optional eager GPU copy for
    single queued capture; inspector: restore policy, capture bandit, min pulls,
    ping-pong, eager copy.

-------------------------------------------------------------------------------

UI text undo — all TMP_InputField (2026-03-24)
-------------------------------------------------------------------------------

Assets/_gm/_Core/UI (reusable)/UiTextUndo/
  • UiTextUndo_MGR + UiTextUndo_Input: RuntimeInitializeOnLoadMethod bootstrap;
    per-field undo/redo stacks; periodic rescan for new TMP fields.
  • Shortcuts while TMP focused: Ctrl/Cmd+Z (undo); Ctrl/Cmd+Y or Ctrl/Cmd+Shift+Z
    (redo). Complements PaintUndo_Input (viewport undo when no field focused).

-------------------------------------------------------------------------------

Command ribbon — Paint tab inactive = like other tabs (2026-03-24)
-------------------------------------------------------------------------------

CommandRibbon_UI.cs
  • Runtime Paint tab: TabBg = invisible raycast plane only; flared sliced sprite
    and colors only on go active (selected), matching prefab tab behavior.
  • CopySlicedTabImageTuningFromReference(activeImg only).
