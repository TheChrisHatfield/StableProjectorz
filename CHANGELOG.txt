StableProjectorz — Change log
==============================

Project version (Unity Player Settings): 2.4.5
This file tracks changes on top of that baseline until the next release bump.

-------------------------------------------------------------------------------

[Unreleased]  |  Date: 2026-03-11  |  Topic: Paint layer stack / inpaint → mesh / SD bridge
-------------------------------------------------------------------------------

Summary
  Multi-layer inpaint display and Stable Diffusion capture now follow the same
  final blit path as the proven single-layer case, with clearer fallbacks and
  resolution alignment to the mesh accumulation texture.

Inpaint / layer display (Inpaint_MaskPainter.cs)
  • ApplyColorLayer_To_UV_Textures: unified flow — resolve a single `source`
    RenderUdims, then one EntireColorLayer blit to accumulation (same pattern
    as Count == 1 using ActiveLayer.Content).
  • CompositeVisibleLayersIntoTemp: builds the multi-layer `source` by blitting
    each visible layer onto a cleared temp with EntireColorLayer_BlitApply (same
    material as single-layer), bottom→top, per-layer opacity; active layer can
    use APPLY_LATEST_BRUSH_TOO while painting.
  • Removed dependency on PaintLayerStack_MGR.CompositeTo for the mesh display
    path (avoids runtime-created stack manager often having no compositor shader
    assigned; CompositeTo / PaintLayer_CompositeBlend remains for other uses).
  • GetLayerCompositeOrFallback: multi-layer path uses CompositeVisibleLayersIntoTemp
    for consistency with viewport/SD mask source; fallbacks to active then layer 0.
  • maskResolution(): prefers accumulation texture width/height when UDIM slice
    count matches; if accumulation not ready, uses SceneResolution_MGR.resultTexQuality
    so layer RTs align with scene UV quality (reduces multi-blit / scale issues).
  • Earlier: removed Inpaint_NoColor + SD-prep early return that skipped layer
    blit during capture; EnsureBottomLayerHasSceneForComposite + scene buffer
    setup for multi-layer; warnings when composite temp or visible Content missing.

Layer stack manager (PaintLayerStack_MGR.cs)
  • CanCompositeLayers public property (_compositeBlendMat != null).
  • CompositeTo: if stack _resolution unset, adopt from first visible layer
    before allocating ping-pong temps; if temps still null, clear dest and log
    instead of leaving stale mask data.

Renderer (Objects_Renderer_MGR.cs)
  • Apply_InpaintSketch_ColorLayer: when layer count > 1, always run
    ApplyColorLayer_To_UV_Textures (UsualView still required); single-layer
    still gated on allowed_to_showBrushMask() so inpaint ribbon modes behave
    as before.

Notes for testers
  • After resolution changes, click in viewport or reload model once so brush
    InitTextures / EnsureResolution refresh layer buffers if sizes changed.
  • If PaintLayer_CompositeBlend fails to load, mesh display no longer depends
    on it; Collapse / mask helpers that still call CompositeTo may log warnings.

Documentation / continual learning
  • Context_Ref/REVIEW_Codebase_Summary.md (sibling folder) — sections 4–5, 9–10
    updated for this paint/layer pipeline.
  • Context_Ref/CONTINUAL_LEARNING.md — pointer to StableProjectorz/continual-learning/.
  • continual-learning/paint-layers-sd-bridge.md — Pattern lines for layer/display/SD
    (folder gitignored).
  • continual-learning/build-stability.md — stub for batch/build patterns (.cursor rule).

-------------------------------------------------------------------------------

Multi-layer paint → SD capture (2026-03-21)
-------------------------------------------------------------------------------

  • EntireColorLayer_BlitApply.shader: Blend One OneMinusSrcAlpha, One One —
    RGB premultiplied-over; alpha additive so CompositeVisibleLayersIntoTemp
    matches PaintLayer_CompositeBlend (fixes weak composite vs accumulation for SD).
  • Inpaint_MaskPainter: forStableDiffusionCapture skips save guard during capture.
  • Objects_Renderer_MGR.EnsureInpaintColorLayerAppliedForCapture: capture flag +
    GL.Flush after ShowFinalMat.
  • SD_GenRequests_Helper: EnsureInpaint + WaitForEndOfFrame before img2img payload
    and before view upscale when genData is null.
  • SD_Generate_PayloadMaker: EnsureInpaint before texture read where applicable.

  Unity packages: Packages/manifest.json and packages-lock.json had no local
  changes at this update (already in sync on main).

-------------------------------------------------------------------------------

Layer rename UI & enumerated collapse names (2026-03-22)
-------------------------------------------------------------------------------

Paint tab — layer list (PaintTab_LayersPanel_UI.cs)
  • Stack still supplies default Layer 1, Layer 2, …; DisplayBlock = read-only label.
  • Click name → EditBlock (TMP_InputField); Enter (onSubmit) commits SetLayerName;
    Escape or focus loss cancels without save.
  • Visibility button also SetActiveLayer so eye can pick active layer.
  • OnActiveLayerChanged → RefreshActiveHighlight only (not full rebuild) so rename
    input is not destroyed on active change.
  • Unity 6 TMP: SelectAll() protected — use onFocusSelectAll on the field.

Layer stack (PaintLayerStack_MGR.cs)
  • SetLayerName, DefaultLayerDisplayName (trim, max length, empty → default).
  • ConsumeNextDefaultCollapseLayerName → Collapse 1, Collapse 2, …
  • CollapseVisibleLayersIntoOne uses enumerated names, not fixed "Collapsed".

Save / load (SerializationObjects.cs + stack Save/Load)
  • PaintLayerStack_SL.nextCollapseNumber; 0 in old saves → InferNextCollapseNumber
    (Collapse N + legacy "Collapsed").

Inpaint (Inpaint_MaskPainter.cs)
  • CollapseLayersIntoScene uses ConsumeNextDefaultCollapseLayerName for the
    single merged layer name (same counter as paint-tab collapse).

-------------------------------------------------------------------------------

Paint undo / redo (session) + Settings UI (2026-03-22)
-------------------------------------------------------------------------------

New module Assets/_gm/Features/Paint/Undo/
  • PaintUndo_MGR: pre-stroke snapshot (GPU copy → async readback → pack → Deflate);
    undo/redo = inflate then amortized per-UDIM GPU upload in LateUpdate;
    PaintUndo_Scheduler: EWMA, LAVD-style aging, optional UCB1 budget.
  • PaintUndo_Input: Ctrl+Z / Ctrl+Y (Cmd+Z on Mac), input-field guard.
  • PaintUndo_Storage: ring buffer; max depth from settings (cap 16).
  • SnapshotRecord + PaintUndo_Compress: lossless format + Deflate;
    TryBuildUncompressedBlob + off-thread Deflate on capture / redo snapshot.
  • LinearSliceUploadOrder for restore upload order (was VisibleFirstOrder).

Hooks / safety
  • Inpaint_MaskPainter: SchedulePreStrokeCapture before brush apply; EnsureExists;
    GetPaintTarget_Undo for restore.
  • MaskPainter: block inpaint while PaintUndo_MGR.BlocksNewStroke.

TextureTools_SPZ.RenderTexture_to_Texture2DList_Async
  • Callback after all slice readbacks finish (incl. errors) via response counter.

Settings (Settings_MGR.cs / Settings_UI.cs)
  • PlayerPrefs: paintUndo_enabled, paintUndo_maxDepth (1–16); restore defaults;
    SyncPaintUndoOnOffLabel for ON/OFF text when toggle set from code.
  • Runtime paint-undo row: Toggle + Image same GameObject, green ON state,
    checkmark, ON/OFF label; max-depth field row.
  • _paintUndoSettingsRowsCreated only after successful row build.
  • VSync runtime row: 112×28 hit area, checkmark, navigation None, label no raycast.
  • SD GPU + paint-undo depth labels: raycastTarget false.
  • AddToggleCheckmarkGraphic helper for runtime toggles.

Documentation
  • docs/PAINT_UNDO_SPEC.md, docs/UNDO_INTEGRATION.md, docs/PAINT_UNDO_PROFILING.md
