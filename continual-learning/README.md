# Continual learning

This folder stores **generalized patterns** learned from past issues so the same class of problem is recognized and handled next time. Prefer pattern derivation over one-off fixes.

## Contents

- **[build-stability.md](build-stability.md)** — Build pipeline and Unity batch mode: invocation surface, batch guards, layers of failure.

When adding new learning: add an **instance** (what happened) and a **→ Pattern:** (reusable principle); extend existing patterns when possible.
