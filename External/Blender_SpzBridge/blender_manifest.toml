schema_version = "1.0.0"
id = "spz_blender_bridge"
name = "SPZ GO (HTTP)"
version = "0.2.0"
tagline = "Blender ↔ StableProjectorz: headless import/export, REST, shared exchange folder (FBX, UV layout)"
maintainer = "StableProjectorz fork / community"
type = "add-on"
blender_version_min = "4.0.0"
license = ["SPDX:MIT"]

[build]
paths_exclude = [".git", "__pycache__", "*.pyc"]
